<UserControl x:Class="InvoiceProject.Views.InvoiceEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1050"
             Background="Transparent"
             UseLayoutRounding="True"
             SnapsToDevicePixels="True"
             TextOptions.TextFormattingMode="Ideal"
             TextOptions.TextRenderingMode="ClearType"
             FontFamily="{StaticResource Poppins}"
             >

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="Auto"/> 
            <RowDefinition Height="*"/>    
        </Grid.RowDefinitions>

        <!-- PAGE TITLE uses the new global style! -->
        <TextBlock Grid.Row="0" Text="Create Invoice" Style="{StaticResource PageTitleStyle}"/>

        <!-- ============================================================== -->
        <!-- TOP CARD -->
        <!-- ============================================================== -->
        <Border Grid.Row="1" Background="{StaticResource SurfaceWhite}" CornerRadius="12" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Direction="270" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.5*"/> 
                    <ColumnDefinition Width="3.5*"/> 
                    <ColumnDefinition Width="1*"/>   
                </Grid.ColumnDefinitions>

                <!-- 1. DOCUMENT DETAILS -->
                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                    <!-- CARD HEADER uses the new global style! -->
                    <TextBlock Text="Document Details" Style="{StaticResource CardHeaderStyle}"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <!-- Inputs use the new BorderLight color -->
                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                            <Label Content="Invoice No."/>
                            <TextBox Text="{Binding InvoiceNumber, UpdateSourceTrigger=PropertyChanged}" Background="#F8FAFC" BorderBrush="{StaticResource BorderLight}" BorderThickness="1"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                            <Label Content="Doc Type"/>
                            <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding SelectedDocumentType}" Background="#F8FAFC" BorderBrush="{StaticResource BorderLight}" BorderThickness="1">
                                <ComboBoxItem Content="100" Tag="100" IsSelected="True"/>
                                <ComboBoxItem Content="380" Tag="380"/>
                                <ComboBoxItem Content="383" Tag="383"/>
                            </ComboBox>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                            <Label Content="Issue Date"/>
                            <DatePicker SelectedDate="{Binding InvoiceDate}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1">
                            <Label Content="Turnover Date"/>
                            <DatePicker SelectedDate="{Binding TurnoverDate}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- 2. BUYER INFORMATION -->
                <StackPanel Grid.Column="1" Margin="0,0,24,0">
                    <!-- CARD HEADER -->
                    <TextBlock Text="Buyer Information" Style="{StaticResource CardHeaderStyle}"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,12,12">
                            <Label Content="Select Saved Client"/>
                            <ComboBox ItemsSource="{Binding AllClients}" SelectedItem="{Binding SelectedClient}" DisplayMemberPath="Name" IsEditable="True" IsTextSearchEnabled="True" Background="#F8FAFC" BorderBrush="{StaticResource BorderLight}" BorderThickness="1"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,12">
                            <Label Content="EDB (Tax No.)"/>
                            <TextBox Text="{Binding BuyerEdb, UpdateSourceTrigger=PropertyChanged}" Background="#F8FAFC" BorderBrush="{StaticResource BorderLight}" BorderThickness="1"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,12,0">
                            <Label Content="Official Name"/>
                            <TextBox Text="{Binding BuyerName}" IsReadOnly="True" Background="#F1F5F9" BorderBrush="{StaticResource BorderLight}" BorderThickness="1"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <Label Content="City"/>
                            <TextBox Text="{Binding SelectedClient.City}" IsReadOnly="True" Background="#F1F5F9" BorderBrush="{StaticResource BorderLight}" BorderThickness="1"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <!-- 3. ACTIONS -->
                <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                    <!-- STATUS MESSAGE: Changed from Teal to Success Green -->
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource BrandSuccess}" FontWeight="Bold" Margin="0,0,0,8" TextWrapping="Wrap" FontSize="11" TextAlignment="Center" Height="28"/>
                    
                    <Button Content="Lookup UJP" Command="{Binding LookupCompanyCommand}" Style="{StaticResource PrimaryActionBtn}" Background="{StaticResource BorderLight}" Foreground="{StaticResource BrandText}" Margin="0,0,0,12"/>
                    <Button Content="Submit Invoice" Command="{Binding SubmitInvoiceCommand}" Style="{StaticResource PrimaryActionBtn}"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ============================================================== -->
        <!-- BOTTOM CARD -->
        <!-- ============================================================== -->
        <Border Grid.Row="2" Background="{StaticResource SurfaceWhite}" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Direction="270" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> 
                    <RowDefinition Height="*"/>    
                    <RowDefinition Height="Auto"/> 
                </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Line Items" Style="{StaticResource CardHeaderStyle}" Margin="0"/>
                    
                    <!-- ADD BUTTON: Changed from Teal to Accent Blue -->
                    <Button Content="+ Add New Line" Command="{Binding AddBlankItemCommand}" Background="Transparent" Foreground="{StaticResource BrandAccent}" FontWeight="SemiBold" HorizontalAlignment="Right" BorderThickness="0" Cursor="Hand"/>
                </Grid>

                <DataGrid Grid.Row="1" ItemsSource="{Binding InvoiceItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,16" BorderThickness="1" BorderBrush="{StaticResource BorderLight}">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Item Name" Binding="{Binding Desc, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        
                        <DataGridComboBoxColumn Header="DDV (%)" SelectedItemBinding="{Binding TaxIndicator, UpdateSourceTrigger=PropertyChanged}" Width="100">
                            <DataGridComboBoxColumn.ItemsSource>
                                <x:Array Type="system:String" xmlns:system="clr-namespace:System;assembly=mscorlib">
                                    <system:String>18%</system:String>
                                    <system:String>5%</system:String>
                                    <system:String>DDV-G</system:String>
                                </x:Array>
                            </DataGridComboBoxColumn.ItemsSource>
                        </DataGridComboBoxColumn>

                        <DataGridTextColumn Header="Qty" Binding="{Binding Qty, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                        <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                        <DataGridTextColumn Header="Row Total" Binding="{Binding RowGrossTotal, StringFormat={}{0:N2}}" IsReadOnly="True" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Totals Footer -->
                <Grid Grid.Row="2">
                    <StackPanel HorizontalAlignment="Right" Width="260">
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Subtotal:" Foreground="{StaticResource TextMuted}" FontSize="13"/>
                            <TextBlock Text="{Binding NetAmount, StringFormat={}{0:N2} MKD}" FontWeight="SemiBold" Foreground="{StaticResource BrandText}" HorizontalAlignment="Right" FontSize="13"/>
                        </Grid>
                        
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Total DDV:" Foreground="{StaticResource TextMuted}" FontSize="13"/>
                            <TextBlock Text="{Binding VatAmount, StringFormat={}{0:N2} MKD}" FontWeight="SemiBold" Foreground="{StaticResource BrandText}" HorizontalAlignment="Right" FontSize="13"/>
                        </Grid>
                        
                        <Rectangle Height="1" Fill="{StaticResource BorderLight}" Margin="0,0,0,12"/>
                        
                        <Grid>
                            <TextBlock Text="Full Price:" FontWeight="Bold" Foreground="{StaticResource BrandPrimary}" FontSize="16" VerticalAlignment="Center"/>
                            
                            <!-- FINAL TOTAL: Changed from Teal to Accent Blue -->
                            <TextBlock Text="{Binding GrossAmount, StringFormat={}{0:N2} MKD}" FontWeight="Bold" Foreground="{StaticResource BrandAccent}" HorizontalAlignment="Right" FontSize="20"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
    </Grid>
</UserControl>