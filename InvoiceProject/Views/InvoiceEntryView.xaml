<UserControl x:Class="InvoiceProject.Views.InvoiceEntryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="720" d:DesignWidth="1050"
             Background="Transparent">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Create Invoice" FontSize="22" FontWeight="Bold" Foreground="#1F2937" Margin="0,0,0,16"/>

        <Border Grid.Row="1" Background="White" CornerRadius="12" Padding="20" Margin="0,0,0,20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Direction="270" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2.5*"/> <ColumnDefinition Width="3.5*"/> <ColumnDefinition Width="1*"/>   </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0" Margin="0,0,24,0">
                    <TextBlock Text="Document Details" FontSize="15" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Margin="0,0,12,12">
                            <Label Content="Invoice No."/>
                            <TextBox Text="{Binding InvoiceNumber, UpdateSourceTrigger=PropertyChanged}" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="1" Margin="0,0,0,12">
                            <Label Content="Doc Type"/>
                            <ComboBox SelectedValuePath="Tag" SelectedValue="{Binding SelectedDocumentType}" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1">
                                <ComboBoxItem Content="100" Tag="100" IsSelected="True"/>
                                <ComboBoxItem Content="380" Tag="380"/>
                                <ComboBoxItem Content="383" Tag="383"/>
                            </ComboBox>
                        </StackPanel>
                        
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,0,12,0">
                            <Label Content="Issue Date"/>
                            <DatePicker SelectedDate="{Binding InvoiceDate}"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="1">
                            <Label Content="Turnover Date"/>
                            <DatePicker SelectedDate="{Binding TurnoverDate}"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="1" Margin="0,0,24,0">
                    <TextBlock Text="Buyer Information" FontSize="15" FontWeight="SemiBold" Foreground="#374151" Margin="0,0,0,12"/>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="1.5*"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>

                        <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,12,12">
                            <Label Content="Select Saved Client"/>
                            <ComboBox ItemsSource="{Binding AllClients}" SelectedItem="{Binding SelectedClient}" DisplayMemberPath="Name" IsEditable="True" IsTextSearchEnabled="True" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"/>
                        </StackPanel>
                        <StackPanel Grid.Row="0" Grid.Column="2" Margin="0,0,0,12">
                            <Label Content="EDB (Tax No.)"/>
                            <TextBox Text="{Binding BuyerEdb, UpdateSourceTrigger=PropertyChanged}" Background="#F9FAFB" BorderBrush="#E5E7EB" BorderThickness="1"/>
                        </StackPanel>

                        <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" Margin="0,0,12,0">
                            <Label Content="Official Name"/>
                            <TextBox Text="{Binding BuyerName}" IsReadOnly="True" Background="#F3F4F6" BorderBrush="#E5E7EB" BorderThickness="1"/>
                        </StackPanel>
                        <StackPanel Grid.Row="1" Grid.Column="2">
                            <Label Content="City"/>
                            <TextBox Text="{Binding SelectedClient.City}" IsReadOnly="True" Background="#F3F4F6" BorderBrush="#E5E7EB" BorderThickness="1"/>
                        </StackPanel>
                    </Grid>
                </StackPanel>

                <StackPanel Grid.Column="2" VerticalAlignment="Bottom">
                    <TextBlock Text="{Binding StatusMessage}" Foreground="{StaticResource BrandTeal}" FontWeight="Bold" Margin="0,0,0,8" TextWrapping="Wrap" FontSize="11" TextAlignment="Center" Height="28"/>
                    
                    <Button Content="Lookup UJP" Command="{Binding LookupCompanyCommand}" Style="{StaticResource PrimaryActionBtn}" Background="#E5E7EB" Foreground="#374151" Margin="0,0,0,12"/>
                    
                    <Button Content="Submit Invoice" Command="{Binding SubmitInvoiceCommand}" Style="{StaticResource PrimaryActionBtn}"/>
                </StackPanel>
            </Grid>
        </Border>

        <Border Grid.Row="2" Background="White" CornerRadius="12" Padding="20">
            <Border.Effect>
                <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.03" Direction="270" Color="Black"/>
            </Border.Effect>
            
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/> <RowDefinition Height="*"/>    <RowDefinition Height="Auto"/> </Grid.RowDefinitions>

                <Grid Grid.Row="0" Margin="0,0,0,12">
                    <TextBlock Text="Line Items" FontSize="15" FontWeight="SemiBold" Foreground="#374151" VerticalAlignment="Center"/>
                    <Button Content="+ Add New Line" Command="{Binding AddBlankItemCommand}" Background="Transparent" Foreground="{StaticResource BrandTeal}" FontWeight="SemiBold" HorizontalAlignment="Right" BorderThickness="0" Cursor="Hand"/>
                </Grid>

                <DataGrid Grid.Row="1" ItemsSource="{Binding InvoiceItems}" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,0,0,16" BorderThickness="1" BorderBrush="#F3F4F6">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Item Name" Binding="{Binding Desc, UpdateSourceTrigger=PropertyChanged}" Width="*"/>
                        
                        <DataGridComboBoxColumn Header="DDV (%)" SelectedItemBinding="{Binding TaxIndicator, UpdateSourceTrigger=PropertyChanged}" Width="100">
                            <DataGridComboBoxColumn.ItemsSource>
                                <x:Array Type="system:String" xmlns:system="clr-namespace:System;assembly=mscorlib">
                                    <system:String>18%</system:String>
                                    <system:String>5%</system:String>
                                    <system:String>DDV-G</system:String>
                                </x:Array>
                            </DataGridComboBoxColumn.ItemsSource>
                        </DataGridComboBoxColumn>

                        <DataGridTextColumn Header="Qty" Binding="{Binding Qty, UpdateSourceTrigger=PropertyChanged}" Width="80"/>
                        <DataGridTextColumn Header="Unit Price" Binding="{Binding UnitPrice, UpdateSourceTrigger=PropertyChanged}" Width="120"/>
                        <DataGridTextColumn Header="Row Total" Binding="{Binding RowGrossTotal, StringFormat={}{0:N2}}" IsReadOnly="True" Width="120"/>
                    </DataGrid.Columns>
                </DataGrid>

                <Grid Grid.Row="2">
                    <StackPanel HorizontalAlignment="Right" Width="260">
                        <Grid Margin="0,0,0,8">
                            <TextBlock Text="Subtotal:" Foreground="#6B7280" FontSize="13"/>
                            <TextBlock Text="{Binding NetAmount, StringFormat={}{0:N2} MKD}" FontWeight="SemiBold" Foreground="#495057" HorizontalAlignment="Right" FontSize="13"/>
                        </Grid>
                        
                        <Grid Margin="0,0,0,12">
                            <TextBlock Text="Total DDV:" Foreground="#6B7280" FontSize="13"/>
                            <TextBlock Text="{Binding VatAmount, StringFormat={}{0:N2} MKD}" FontWeight="SemiBold" Foreground="#495057" HorizontalAlignment="Right" FontSize="13"/>
                        </Grid>
                        
                        <Rectangle Height="1" Fill="#E5E7EB" Margin="0,0,0,12"/>
                        
                        <Grid>
                            <TextBlock Text="Full Price:" FontWeight="Bold" Foreground="#1F2937" FontSize="16" VerticalAlignment="Center"/>
                            <TextBlock Text="{Binding GrossAmount, StringFormat={}{0:N2} MKD}" FontWeight="Bold" Foreground="{StaticResource BrandTeal}" HorizontalAlignment="Right" FontSize="20"/>
                        </Grid>
                    </StackPanel>
                </Grid>
            </Grid>
        </Border>
        
    </Grid>
</UserControl>